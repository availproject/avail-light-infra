name: Deploy Lightclients

on:
  push:
    branches: [ "**" ]

jobs:
  hex:
    uses: ./.github/workflows/common.yml
    with:
      network: hex
    secrets:
      datadog_api_key: ${{ secrets.DATADOG_API_KEY }}